PUBLIC M5
EXTRN M4:NEAR

CSEG SEGMENT PARA PUBLIC 'CODE'
	ASSUME CS:CSEG

M5 PROC
	PUSH BX
	PUSH BP
	MOV BP, SP
	MOV DX, [BP + 6]
	CMP DX, 0
	JGE DIGITS
NEGATE:
	PUSH DX
	MOV AH, 2
	MOV DL, '-'
	INT 21h
	POP DX
	NEG DX
DIGITS:
	PUSH DX
	CALL M4
	POP BP
	POP BX
	RET 2
M5 ENDP

CSEG ENDS
	END