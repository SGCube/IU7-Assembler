PUBLIC M3
EXTRN M2:NEAR

CSEG SEGMENT PARA PUBLIC 'CODE'
	ASSUME CS:CSEG

M3 PROC
	PUSH BX
	PUSH BP
	MOV BP, SP
	MOV DX, [BP + 6]
	CMP DX, 0
	JGE DIGITS
NEGATE:
	PUSH DX
	MOV AH, 2
	MOV DL, '-'
	INT 21h
	POP DX
	NEG DX
DIGITS:
	PUSH DX
	CALL M2
	POP BP
	POP BX
	RET 2
M3 ENDP

CSEG ENDS
	END