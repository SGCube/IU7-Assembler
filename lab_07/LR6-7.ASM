PUBLIC M7
EXTRN M6:NEAR

CSEG SEGMENT PARA PUBLIC 'CODE'
	ASSUME CS:CSEG

M7 PROC
	PUSH BX
	PUSH BP
	MOV BP, SP
	MOV DX, [BP + 6]
	CMP DX, 0
	JGE DIGITS
NEGATE:
	PUSH DX
	MOV AH, 2
	MOV DL, '-'
	INT 21h
	POP DX
	NEG DX
DIGITS:
	PUSH DX
	CALL M6
	POP BP
	POP BX
	RET 2
M7 ENDP

CSEG ENDS
	END